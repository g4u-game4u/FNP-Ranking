{
  "mcpServers": {
    "n8n": {
      "command": "npx",
      "args": ["-y", "n8n-mcp@latest"],
      "env": {
        "N8N_API_URL": "https://fnp.centralsupernova.com.br",
        "N8N_API_KEY": "your_n8n_api_key_here"
      },
      "disabled": true,
      "autoApprove": [],
      "comment": "N8N automation server - add your API key and enable when ready"
    }
  },
  "comment": "Self-hosted Supabase does not have MCP server. Use Supabase JS client library directly (already installed and configured in src/services/supabaseApi.ts)"
}
